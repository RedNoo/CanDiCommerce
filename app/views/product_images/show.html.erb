<p id="notice"><%= notice %></p>

<p>
  <strong>Product:</strong>
  <%= @product_image.product.title %>
</p>

<p>
  <strong>Order no:</strong>
  <%= @product_image.order_no %>
</p>
<%= link_to 'Edit', edit_product_image_path(@product_image) %> |

<%= link_to 'Back', product_image_list_path(@product_image.product) %>
<p>
        <% unless @product_image.data.nil? %>
                <%= ('<img class="img-fluid"   src="data:@employee.mime_type;base64,%s">' % Base64.encode64(@product_image.data)).html_safe %>
                <% end %>



</p>

